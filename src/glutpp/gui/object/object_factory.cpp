

#include <glutpp/gui/object/object_factory.h>

std::shared_ptr<glutpp::gui::object::object>    glutpp::gui::object::object_factory::create(TiXmlElement* element) {
  
  assert(element);
  
  char const * buf = element->get_text();
  
  if()
  {
  }
  else if()
  {
  }
  else
  {
  }
  
}
